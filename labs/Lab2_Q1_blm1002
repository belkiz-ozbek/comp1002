#include<stdio.h>

int main(){
	
int number,i;
double result = 0.0;
int sign = 1;

scanf("%d", &number);

	for(i = 1; i <= (2 * number - 1); i += 2){
	
	result += (4.0 / i) * sign;
	
	sign *= -1;
		
	}

printf("%.6lf", result);	
}
